import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appgravity.registrodegastos',
  appName: 'FIN$INK',
  webDir: 'dist'
};

export default config;
